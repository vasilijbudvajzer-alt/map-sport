services:
  - type: web
    name: russia-running-map
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000